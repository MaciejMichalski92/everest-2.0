import theme from '@/components/theme';
import styled from 'styled-components';

export const StyledSearchComponent = styled.div`
  width: 80%;
  margin: 50px auto;
  padding: 10px;
  border-radius: 5px;
  background-color: ${theme.colors.sapphireBlue};
`;
